#include<stdio.h>
int main(void)
{
    int i, n, avg, a, b, c;
    double m;

    scanf("%d", &n);
    for(i=1; i<=n; i++)
    {
        scanf("%lf", &m);
    }

    for(i=1; i<=n; i++)
    {
       printf("%lf\n", )
    }


        return 0;
}
